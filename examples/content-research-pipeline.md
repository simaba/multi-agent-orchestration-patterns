# Example: Content Research Pipeline

A simple pattern choice for content research:
- parallel retrieval agents gather material,
- an aggregator combines the findings,
- a drafting agent creates a synthesis,
- an evaluator checks for gaps.

This example shows how orchestration patterns can be combined while preserving clear control logic.
